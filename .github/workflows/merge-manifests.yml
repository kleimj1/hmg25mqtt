name: Merge Manifests

on:
  workflow_call:
    inputs:
      registry:
        required: true
        type: string
      image-suffix:
        required: true
        type: string
      digest-prefix:
        required: true
        type: string

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download digests
        uses: actions/download-artifact@v4
        with:
          name: ${{ inputs.digest-prefix }}linux-amd64
          path: /tmp/digests/linux-amd64

      - name: Download digests arm64
        uses: actions/download-artifact@v4
        with:
          name: ${{ inputs.digest-prefix }}linux-arm64
          path: /tmp/digests/linux-arm64

      - name: Download digests armv7
        uses: actions/download-artifact@v4
        with:
          name: ${{ inputs.digest-prefix }}linux-arm-v7
          path: /tmp/digests/linux-arm-v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ inputs.registry }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create and Push Manifest List
        run: |
          IMAGE="${{ inputs.registry }}/${{ github.repository }}${{ inputs.image-suffix }}"
          REF="${GITHUB_REF_NAME}"

          echo "🔨 Building manifest list for $IMAGE:$REF"

          docker buildx imagetools create \
            -t "$IMAGE:$REF" \
            -t "$IMAGE:latest" \
            "$IMAGE@$(cat /tmp/digests/linux-amd64/digest.txt)" \
            "$IMAGE@$(cat /tmp/digests/linux-arm64/digest.txt)" \
            "$IMAGE@$(cat /tmp/digests/linux-arm-v7/digest.txt)"

      - name: Inspect Manifest List
        run: |
          docker buildx imagetools inspect "${{ inputs.registry }}/${{ github.repository }}${{ inputs.image-suffix }}:latest"
